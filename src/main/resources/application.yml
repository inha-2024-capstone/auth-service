spring:
  application:
    name: authserver
  datasource: # mysql
    url: jdbc:mysql://localhost:3306/kim
    username: root
    password: 12345
  jpa: # jpa settings
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
